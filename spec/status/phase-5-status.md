# Phase 5 status — DOM selection and temporary width preview

Status: **Implementation complete on Windows; full cross-platform and cross-browser gate not verified.**

This is the single canonical Phase 5 traceability matrix. Phase 6 source mapping and source writing remain untouched.

| Test ID | Requirement and expected result | Test type | Test location | Execution command | Windows environment | Windows result | Windows evidence | Unix environment | Unix result | Unix evidence | Overall status | Notes or blocker |
| ------- | ------------------------------- | --------- | ------------- | ----------------- | ------------------- | -------------- | ---------------- | ---------------- | ----------- | ------------- | -------------- | ---------------- |
| P5-01 | Select the Vanilla Annual card and resize 320px to 420px; only it previews 420px, the display reports 420px, and disk is unchanged. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Real Vanilla/Vite pointer drag produced 320px → 420px; starter remained 280px; stylesheet SHA-256 unchanged; final suite 15/15, exit 0. | Unavailable: no WSL distribution, container daemon, VM, or authenticated CI | Not verified | No executable Unix environment. | Partially verified | Minimal prerequisite repair corrected the Vanilla target's CSS specificity so its specified width is actually 320px. |
| P5-02 | Cancel and Escape restore exact inline width presence, value, and priority; handles and pending proposal disappear. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e`; post-review focused P5-02 | Windows native, installed Chrome | Passed | Both `320px !important` and an absent property restored exactly; selection/panel removed; final suite and post-review focused rerun passed, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Uses CSSStyleDeclaration `item()` for cross-browser-safe presence detection. |
| P5-03 | Apply on the real React Annual card sends one immutable complete proposal with IDs/context; server acknowledges it and no file changes. | Integration, End-to-end | `tests/integration/phase-5-proposal.test.ts`; `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:integration`; `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome, real React/Vite | Passed | One deeply frozen proposal with session/tab/generation/proposal IDs and fingerprint/original context was acknowledged; React stylesheet hash unchanged; 3/3 integration and 15/15 E2E, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Restricted sandbox could not transform React; the same test passed with normal approved Windows dependency access. |
| P5-04 | A second selection with an active preview requires explicit discard and never leaves two live/hidden previews. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Confirmed discard restored Annual to 320px and left exactly one Enterprise selection/overlay; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Explicit browser confirmation is the Phase 5 policy. |
| P5-05 | Border-box, percentage, min/max, and transformed geometry track the real rect; values are finite/clamped and proposal context is source-independent. | End-to-end, Integration | `tests/e2e/phase-5-selection.spec.ts`; `tests/integration/phase-5-proposal.test.ts` | `npm.cmd run test:phase5:e2e`; `npm.cmd run test:phase5:integration` | Windows native, installed Chrome | Passed | Constraint rect clamped to 450px; overlay matched translated rect within 2px; NaN left DOM unchanged; proposal context was schema-validated; exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | No source selector or path is produced. |
| P5-06 | Nested scroll and viewport-edge overlays/controls reposition and remain usable. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Nested horizontal/vertical scroll repositioned selection; panel stayed fully inside viewport; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Scroll and resize use one rAF geometry update. |
| P5-07 | Reframe UI, head/script/style, hidden/zero-area, canvas, and cross-origin iframe targets do not replace selection or create a proposal, and explain rejection. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | All unsupported targets left the original selection ID unchanged and proposal count zero; Reframe brand click did not enter page selection; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Cross-origin iframe element is excluded; inaccessible frame content is never inspected. |
| P5-08 | NaN, Infinity, negative, zero, extreme, malformed, or invalid-session widths leave DOM/server unchanged and are rejected. | Integration, End-to-end | `tests/integration/phase-5-proposal.test.ts`; `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:integration`; `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Real server rejected NaN, Infinity, -1, 0, and 10001 with zero accepted proposals; devtools NaN/Infinity left DOM unchanged; both suites exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Phase 4 legacy apply remains accepted only as inert/non-writing traffic for regression compatibility. |
| P5-09 | Removing the selected node during drag cancels safely, detaches overlays, disables Apply, and leaves page usable. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Mutation observer cleared removed selection during active pointer drag; after Select-off, native control worked; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | No detached-node exception or leftover overlay. |
| P5-10 | HMR replacement and route change invalidate generations; stale Apply is rejected and reselection is required. | Integration, End-to-end | `tests/integration/phase-5-proposal.test.ts`; `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:integration`; `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Node replacement cleared selection, reselection advanced generation, route push cleared it again; real server rejected stale generation with `SELECTION_STALE`; exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | History methods are restored on Exit. |
| P5-11 | Cancel after unrelated inline application changes restores only width and preserves the unrelated change. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Cancel restored width to 320px while preserving `rgb(1, 2, 3)` background; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Restoration touches only the inline width property. |
| P5-12 | Disconnect during resize keeps the preview labeled temporary, disables Apply, and reconnect never replays it as a mutation. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Real endpoint shutdown showed Disconnected/Temporary, disabled Apply, ignored a dispatched click, reconnected, and accepted zero proposals; exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Reselection state is re-announced only after connection; no edit is queued. |
| P5-13 | With Select off, links, inputs, menus, buttons, and scrolling behave like upstream. | End-to-end, Regression | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Input value, select option, button action, hash link, and scroll all behaved normally; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Document capture listeners are inert outside Select mode. |
| P5-14 | Hard refresh during preview returns to source width with no saved marker or replay. | End-to-end | `tests/e2e/phase-5-selection.spec.ts` | `npm.cmd run test:phase5:e2e` | Windows native, installed Chrome | Passed | Active 420px preview returned to source 320px after hard reload; no saved marker existed; final suite 15/15, exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | Preview state is memory-only. |
| P5-15 | Chromium plus Firefox and WebKit have equivalent hover/select/resize/cancel behavior. | Cross-browser, End-to-end | `tests/e2e/phase-5-selection.spec.ts`; `playwright.phase5.config.ts` | `npm.cmd run test:phase5:cross-browser` | Windows native | Partially verified | Installed Chrome/Chromium passed. Firefox executable `firefox-1532` and WebKit executable `webkit-2311` do not exist; command result 1 passed, 2 unavailable, exit 1. | Unavailable | Not verified | No executable Unix environment. | Partially verified | CI installs and runs all three browsers, but no remote result is claimed because GitHub authentication is invalid. |
| P5-16 | On 2,000 nodes and a five-second drag, no >50ms selection long task occurs; p50/p95 are recorded and preview messages remain bounded. | Performance, End-to-end | `tests/e2e/phase-5-performance.spec.ts` | `npm.cmd run test:phase5:performance` | Windows native, installed Chrome | Passed | Final run: 322 samples, p50 0.20ms, p95 0.30ms, max 0.40ms, no long tasks, 46 preview messages, 2,000 nodes, 5,029ms drag, 220 pointer moves; exit 0. | Unavailable | Not verified | No executable Unix environment. | Partially verified | p50/p95 are observability-only; bounded traffic/no long task are functional checks. |
| P4-01 | Regression: ready/ping/pong and Test Connection still report real connectivity. | Regression, End-to-end | `tests/e2e/phase-4-connection.spec.ts` | `npm.cmd run test:phase5:regression` | Windows native, installed Chrome | Passed | Included in complete Phase 4 browser run 6/6, exit 0. | Unavailable | Not verified | Prior Unix limitation retained. | Partially verified | Phase 5 dependency remains valid. |
| P4-suite | Regression: complete Phase 4 schema/socket/browser/performance and targeted earlier-phase suite remains green. | Regression | Existing Phase 4 suites | `npm.cmd run test:phase5:regression` | Windows native | Passed | Build; P4 integration 7/7; P4 browser 6/6; P4 perf 1/1; P3 WS 1/1; P3 browser 5/5; P2 1/1; packed P1 group 4/4; exit 0. | Unavailable | Not verified | Prior Unix limitation retained. | Partially verified | Phase 4 protected digest unchanged. |
| P3-12 | Regression: Exit removes Reframe UI/listeners/offsets and restores normal page behavior after route/HMR. | Regression, End-to-end | `tests/e2e/phase-3-toolbar.spec.ts`; Phase 5 teardown | `npm.cmd run test:phase5:regression` | Windows native, installed Chrome | Passed | Targeted Phase 3 browser group 5/5 including P3-12; exit 0. | Unavailable | Not verified | Prior Unix limitation retained. | Partially verified | Phase 5 additionally restores preview width, history hooks, observer, rAF, and pointer listeners. |

## Implementation and changed files

- `packages/browser-client/src/index.ts` — modified existing Phase 4 client: Select mode, eligibility, overlays, one-selection policy, rAF resize, exact restoration, stale/removal/disconnect handling, proposal Apply, teardown, and bounded performance metrics.
- `packages/shared/src/protocol.ts` — modified existing uncommitted Phase 4 file: bounded fingerprint/original-width schemas, full proposal, acknowledgement, and stale-selection code; retained inert Phase 4 apply compatibility.
- `packages/dev-server/src/websocket-server.ts`, `packages/dev-server/src/proxy.ts` — modified existing uncommitted files: generation validation, duplicate protection, immutable proposal callback, acknowledgement, and safe diagnostics; no filesystem import or write.
- `demo/vanilla-demo/style.css` — modified as a labeled prerequisite repair: the plan's Annual target now truly computes to 320px rather than losing to a more specific 280px rule.
- `tests/integration/phase-5-proposal.test.ts`, `tests/e2e/phase-5-selection.spec.ts`, `tests/e2e/phase-5-performance.spec.ts` — created for P5-01 through P5-16.
- `scripts/run-phase5-tests.mjs`, `playwright.phase5.config.ts`, `spec/status/phase-5-status.md` — created for orchestration, cross-browser execution, and this canonical record.
- `package.json`, `.github/workflows/ci.yml`, `README.md` — modified existing Phase 0–4 files to expose Phase 5 commands, Windows/Ubuntu plus three-browser CI, and current behavior.
- No dependency was added. `package-lock.json` retained its pre-existing Phase 4 dirty state and was not changed by Phase 5 work.

All other dirty/untracked Phase 0–4 files listed by `git status` were pre-existing user/agent work and were preserved.

## Operating-system and browser discovery

| Environment | Inspection or attempt | Result |
| --- | --- | --- |
| Windows native | `node -p ...`, installed Chrome, real loopback/Vite/process tests | Available; Node 24.14.0 win32-x64. Phase 5 and regressions passed. |
| WSL | `wsl.exe --list --verbose` | Exit 1; no installed distribution. Installing one is out-of-scope host provisioning. |
| Docker/Linux | `docker.exe info --format '{{.ServerVersion}}'` | Exit 1; no daemon pipe and inaccessible config. |
| Other local VM/container tools | `Get-Command podman.exe,multipass.exe,VBoxManage.exe` | Commands absent. |
| GitHub remote CI | workflow inspection; `gh.exe auth status` | Windows/Ubuntu matrix is configured, but the token is invalid; no remote result claimed. |
| Chromium | system Chrome plus `npm.cmd run test:phase5:cross-browser` | Passed P5-15. |
| Firefox/WebKit | Playwright cache inspection and cross-browser command | Not installed; launch failed before test execution. CI is prepared to install both. |

## Command evidence

- Preparation: build passed; Phase 4 integration passed 7/7; P4-01 passed 1/1. One multi-ID npm grep command exited 1 because Windows `cmd` interpreted `|`; no test ran and no product failure was claimed.
- Protocol increment: build passed; Phase 4 integration remained 7/7; Phase 5 proposal integration passed 3/3.
- P5-01 first exposed the Vanilla specificity defect (actual 280px); the minimal selector repair made the exact 320→420 test pass.
- First full E2E attempt: 13 passed, 2 failed. P5-03 exposed restricted-sandbox React/esbuild access and passed with normal approved Windows access. P5-09 exposed that its test clicked the app while Select intentionally remained on; the test now turns Select off before asserting native behavior.
- Second full E2E attempt: 14 passed, P5-15 failed. This exposed deferred use of `Event.composedPath()` after dispatch; the client now snapshots the path before rAF. Focused P5-15 then passed.
- First performance attempt measured a nominal loop that actually lasted about 15 seconds, yielding 154 messages. The workload now terminates by measured five-second wall time; final metrics are in P5-16.
- `npm.cmd run test:phase5:e2e`: 15 passed, 0 failed, 0 skipped, exit 0.
- `npm.cmd run test:phase5:cross-browser`: Chromium passed; Firefox/WebKit unavailable before execution; exit 1.
- `npm.cmd run test:phase5`: build, integration 3/3, E2E 15/15, performance 1/1, and complete Phase 4 regression runner all passed; both Phase 4 and Phase 5 protected-tree SHA-256 digests were `a0309665bdc9ed863ae6c3d12167bd678d7e5f1a3e56085f583e70a9e012114f` before and after; exit 0.
- Post-review `npm.cmd run build` and focused P5-02 passed after the cross-browser-safe width-presence cleanup; exit 0.
- `git diff --check`: exit 0; only existing LF/CRLF conversion notices.
- Process cleanup: only the three Node processes that predated final verification remain. The one owned runner from an earlier timed React attempt was stopped; no owned listener or temporary fixture remains.

## Performance observations

| Metric | Gate type | Workload | Samples | Environment | p50 | p95 | Other result | Budget and effect |
| --- | --- | --- | ---: | --- | ---: | ---: | --- | --- |
| Selection/preview frame work | Observability-only | 2,000 nodes; 5,029ms real pointer drag | 322 | Windows native, installed Chrome | 0.20ms | 0.30ms | max 0.40ms | 8ms p95 budget; within budget, but not converted to a hard gate. |
| Long task and traffic bound | Functional | Same run, 220 pointer moves | 220 moves | Same | n/a | n/a | no >50ms long task; 46 preview messages | Passed; no UI stall or unbounded message stream. |

## Acceptance, regressions, and stopping point

- Hover/one-selection behavior in real Vanilla and React demos: **Passed on Windows; Unix Not verified** (P5-01, P5-03, P5-15).
- Unsupported target safety: **Passed on Windows; Unix Not verified** (P5-07).
- Temporary smooth resize and constrained geometry: **Passed on Windows; Unix Not verified** (P5-01, P5-05, P5-06, P5-16).
- Exact Cancel/Escape/refresh restoration: **Passed on Windows; Unix Not verified** (P5-02, P5-11, P5-14).
- Complete immutable non-writing Apply: **Passed on Windows; Unix Not verified** (P5-03, P5-08, P5-10, P5-12).
- Select-off/Exit leaves normal app behavior and no leftovers: **Passed on Windows; Unix Not verified** (P5-13, P3-12).
- Dynamic DOM, route/HMR, scroll, edge, disconnect, and removal recovery: **Passed on Windows; Unix Not verified** (P5-06, P5-09, P5-10, P5-12).
- Phase 4 regression status: **Passed on Windows; Unix remains Not verified**. No regression detected.
- Cross-platform gate: **Implementation complete on available OS; other OS Not verified**.
- Cross-browser gate: **Partially verified**; Chromium passed, Firefox/WebKit are Not verified.
- Known limitations: no executable Unix environment; no local Firefox/WebKit binaries; no authenticated remote CI run. The `NO_COLOR`/`FORCE_COLOR` warning is environmental and not a product defect.
- Final verified capability: one eligible page element can be selected and temporarily resized; cancellation is exact; Apply sends one validated immutable proposal and restores the page without touching source.
- Last verified named test ID: P5-16. Phase 6 remains untouched.
- Continue only with an explicit instruction such as `Start Phase 6 only`, `install and rerun Firefox/WebKit`, or `verify Phase 5 on Unix`.
